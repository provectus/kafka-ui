auth:
  enabled: false
management:
  endpoint:
    info:
      enabled: true
    health:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "info,health"
#spring:
#  main:
#    allow-bean-definition-overriding=true: