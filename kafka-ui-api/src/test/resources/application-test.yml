kafka:
  clusters:
    -
      name: local
      bootstrapServers: localhost:9093
      zookeeper: localhost:2181
      schemaRegistry: http://localhost:8081
      jmxPort: 9997
spring:
  jmx:
    enabled: true
auth:
  type: DISABLED